
<%
    var classifica = database[0].coppaChiosco

    classifica.sort((a,b) =>  a.qty - b.qty )
%>
<script class="nuovaClassifica">
    var nuovaClassifica = []
    <% 
        for(var i=classifica.length-1; i>=0; i--) {
    %>
        
            nuovaClassifica.push({
                squadra: '<%=classifica[i].nome%>',
                punti: <%=classifica[i].qty%>,
                id: '<%=classifica[i].nome.replace(/ /g, '_')%>'
            })
        
    <%
        }
    %>
</script>
    